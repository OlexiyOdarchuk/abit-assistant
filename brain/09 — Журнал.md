---
tags: [project/abit-assistant, doc/log]
---

# 09 — Журнал

У зворотньому хронологічному (найновіше зверху). Один рядок ≈ один коміт + контекст. Для повного diff'у — `git show <sha>`.

## 2026-06-13 · Передкампанійний аудит + план фіч

- `47320e9` **fix(bot): preflight broadcast + дедуп логів**. Розсилка з кривим Markdown давала 400 на КОЖНОМУ юзері (N тихих фейлів). Тепер текст тест-сендиться адміну першим; помилка парсингу скасовує розсилку з plain-text діагностикою. Окремо: `logUpdates` (зовнішній middleware) і telebot `OnError` дублювали кожну помилку — `OnError` опущено до Debug (backstop, не другий логер).
- `b58ff6c` **fix(osvita): кап 200k рядків проти OOM**. `fanOut` додавав чанки без стелі; зламаний upstream міг з'їсти пам'ять. Перевищення скасовує всі лейни + помилка.
- `e4ce8e9` **chore**: `go mod tidy` (go-chart, x/sync → direct), видалив мертві `btnUniqueDonate`/`maxCreative`, README `/lists` готовий.
- **Спростовано 2 «знахідки» зі старого аудиту**: `.env` уже в `.gitignore` (трекається лише `.env.example`); СБ-у-загальному-пулі вже виправлено в `8d56e78`. Нотатки в brain були застарілі.
- **Roadmap**: додано секцію [[08 — Roadmap#🎯 Великі фічі — «максимально спростити життя вступнику»|🎯 Великі фічі]] — симуляція пріоритетів (через `EnrichService`, закриває діру точності з [[10 — Правила вступу і формули]]), реверс-пошук «куди я поступлю», оптимізатор пріоритетів.
- **Deep research** (103 агенти, верифікація 2/3) по алгоритму вступу + osvita URL. Висновки внесено в [[10 — Правила вступу і формули#Пріоритети та адресне розміщення|10 — алгоритм]] і [[05 — Парсери і сервіси#URL-структура (deep research 2026-06-13)|05 — URL/spec]]. Ключове: (1) **між абітурієнтами виграє бал, не пріоритет** → евристика «зняти конкурента» коректна; (2) **2026: 10 заяв / 5 бюджет** (стара «15» спростована); (3) **r14 = Львів, не Харків**; 282 = код закладу; (4) **`/spec/` browse-endpoint існує** → реверс-пошук розблоковано. Заодно підчищено 2 застарілі місця в doc 05 (bandwidth cap → зроблено; ratchet → видалено в `acc15e1`).

## 2026-06-13 (ч.2) · Фіча №2 «Куди я вступлю» + полір

- `5a8dc73` **feat(bot): /where** — галузь → регіон → ранжований список бюджетних програм, тап → повний аналіз. `DiscoverService` у графі бота.
- `77a7b14` **полір**: мультивибір областей, summary 🟢/🟡/🔴, «➕ Ще», «🎯 тільки прохідні», «💾 надійні в /lists». `DiscoverService` розбито на `Browse`(мульти-фільтр мердж)+`Analyze`(зріз)+`SortMatches`; `abit.ChanceLevel.Tier()`.
- `74a6a78` **спеціальність + back**: фільтр спеціальності з результатів (мітки з буквами, без AJAX); спільний `discoverView` (індекси кнопок=відкрита програма); back з програми → результати через фільтр у `UserSettings` + re-run з кешу.
- Рефактор `pkg/→internal/` (вище) + прибрано співавторство з історії — усе цього ж дня.

## 2026-06-13 (ч.3) · Симулятор пріоритетів №1 + дрібниці №2

- `50541b9` бюджет/контракт тогл у /where; `17e2ea6` букви галузей A–K у пікері (мапа industryId→літера, емпірично з /spec/).
- Endpoint спеціальностей `/api/?get=specialities` знайдено, але `[]` off-season → up-front вибір відкладено; post-browse фільтр покриває.
- `7d62518` **feat: симулятор пріоритетів №1**. `PrioritySimulator` (`simulate.go`): конкуренти вище юзера з пріоритетом ≥2 → abit-poisk → знятий, якщо проходить на вищий пріоритет (статус рекомендовано/наказ) → `OverrideMap` → перерахунок Analyze. Кнопка «🔮 Уточнити» на summary: baseline→refined. Status-сигнал надійний з початком хвиль; pre-wave прогноз (fetch+rank вищих пріоритетів) — наступний крок, валідувати на живих даних. → [[08 — Roadmap]].

## 2026-06-13 (ч.4) · Pre-wave прогноз для №1

- `f1e7e59` **feat: pre-wave прогноз**. `Resolver` (`resolver.go`): abit-poisk (ВНЗ, спец) → osvita URL через довідник `universities_0.json` (`MatchUniversity`) + `/spec/` по universityId (кеш) + match спеціальності (відкидаю код-префікс F3/I10; міс → false, не вгадує). `PrioritySimulator.predictDeparture`: для не-рекомендованих конкурентів резолвимо/фетчимо/ранжуємо їх вищі пріоритети їхнім балом → проходить → знятий (Predicted). `predictCap=12`. UI на summary: 🔮 прогноз vs ✅ підтверджено. Тести з фейк-resolver/fetcher + кеш браузу. ⚠️ матчинг спеціальностей крихкий (різні формати abit-poisk/osvita) — консервативно, точність валідувати на живих 2026. → [[08 — Roadmap]].

## 2026-06-13 (ч.5) · Великий аудит сесійного коду

4 паралельні рев'ю-агенти (симулятор+резолвер, discover, парсери, бот/FSM). Багато перевірено як безпечне; реальні фікси — коміт `404e6ca`:
- **bot**: back-to-results губився при ре-рендері summary → персистентний прапор `from_discover` у FSM (renderSummary + showResultsPage). «➕ Ще» брехав при >60 збігах / dropped-fetch → hasMore та повідомлення на основі analyzedTarget vs storedCount(≤60). «💾 Надійні» тепер з фільтрованого view. Прибрано мертвий `setDiscoverPage`/`d_page`.
- **simulate**: `isPlacedStatus` спершу виключає негативи («виключено/відраховано з наказу» містять «наказу»); `isPassingChance` → лише ChanceHigh (квота-кейси недосяжні без квот конкурента).
- **resolver**: транзитна помилка `FetchUniversities` більше не кешується назавжди; матчинг спеціальності exact-first + token-subset (не сирий substring — «Право» ≠ «Правознавство»); `MatchUniversity` containment вимагає ≥5 рун overlap.
- Нові тести на ці кейси. Vet+test зелені.

## 2026-05-22 → 23 · Завершальний день

- `9b9f58b` **fix(edbo): real AES-CBC формула з vstup2025/js/functions.js**. Через Playwright capture знайшли точну формулу — `salt = "v" + str(a*b)`, не `(7500-prsid)*n` як placeholder. Refactor `internal/edbo/crypto.go`: `Decrypt(blob, salt, year)`, `SaltMultiply(a, b)`, `DecryptName(blob, prsid, n, year)`, `Encrypt(...)` для тестів. 5 round-trip cases. → [[06 — EDBO Research]]
- `f8eb3f2` **fix(config): default DB path → XDG**. Раніше `./data/abit.db` губився при різних cwd (і при `rm -rf data/` у моїх smoke-тестах). Тепер `$HOME/.local/share/abit-assistant/abit.db`. Startup-лог показує абсолютний шлях + кількість юзерів.
- `304ecd3` **fix(parser/osvita): Volume з `<tr><td>k</td><td>v</td></tr>`**. Раніше шукали `<b>` всередині `.block-pro-vnz` — на реальній сторінці матчилось нуль. Тепер скрейпимо table-rows. 16 полів замість 0 → Analyze дає реальний бюджет замість ChanceUnknown.

## 2026-05-22 · Великий refactor (security + correctness audit)

5 коммітів C1-C5 за результатами двох парало-agent аудитів.

- `f6192b7` **C5 cleanup**: broadcast lifecycle прив'язаний до rootCtx (SIGTERM скасовує), WithInsecureTLS мутує transport не replace client, reportErrors фільтрує infrastructure errors, dead Distribution branch коментар.
- `3363e59` **C4 concurrency + storage**: singleflight на ProgramService.Fetch (stampede на viral share), osvita fanOut ctx cancel на firstErr, DSN busy_timeout=5s, trackUser 5s + fatal-on-fail (FK violations downstream).
- `8e9dd70` **C3 FSM hygiene + correctness**: IsCompetitor `>` → `>=` (tie = competitor як в Python), Volume fallback → ChanceUnknown замість фабрикації, isMaskedName тільки `###`, OtherReq показується лише з RecType, RegionCoef warning, Refresh diff показує UserScore, FSM clear на menu nav.
- `c53fc40` **C2 HIGH UX**: edit-NMT-score кнопка була no-op (дублювала open), додано окремий btnUnique. recoverPanics innermost middleware (інакше panic перетинає reportErrors). applicantButtonLabel + truncated → rune-based truncation (Cyrillic mid-codepoint cut ламав Telegram).
- `66b2a08` **C1 critical security**: deep-link `list_<id>` дозволяв enumerate чужі списки → змінено на `share_<token>` з 128-bit random. mdEscape повний (\\, *, _, `, [, ]). looksLikeOsvitaURL строгий host check. Private-chat gate на /search, /profile, /lists, /admin (анти-leak userScore у група).

## 2026-05-22 · Docker + Admin

- `298134e` **feat: scratch Docker + Compose, non-root, hardened**. Multi-stage, golang:1.26.3-alpine → scratch, CGO_ENABLED=0, 12.9 МБ. compose з cap_drop:ALL, read_only, no-new-privileges, 256M limit.
- `ace10ed` **feat(bot): /admin panel** — stats, broadcast (rate-limited 20/sec), vacuum cache. Goroutine з рейт-лімітом ~50ms/повідомлення.

## 2026-05-22 · Visuals + lists

- `6abb781` docs: roadmap update
- `ee32116` **feat(bot,visualizer): histogram PNG на summary**. go-chart/v2 BarChart, кольори за відношенням до userScore (🟢🟡🔴⚪).
- `38e8d50` **feat: manual toggle «конкурент / не конкурент»**. OverrideMap у FSM, sticky між page-flips. Detail screen показує italics-cue коли override активний.
- `3db1679` **feat: saved lists v2** — refresh + diff "було → стало", share через deep-link (тоді ще id, не token), JSON export, delete confirmation.
- `4329817` **feat: saved lists v1** — save snapshot, list, view, delete.
- `39d7158` **feat: summary screen** — після /search не одразу список, а екран з місцем/шансом/вердиктом (port Python).

## 2026-05-22 · Profile + bot foundation

- `0b95098` chore: gitignore /data/
- `e89f59a` fix: NMT subject grid duplicate last pair (row[:0] = backing array reuse, regression test додано)
- `238eccb` **fix: 3+1 NMT rule, weighted-avg rating, smarter competitor filter**. Profile cap 4 предмети, required 🔒 marker, ComputeRating як Python's weighted average. IsCompetitor mirror filter.py.
- `0b84faf` **feat: user rating + competitor markers + toggle**. internal/abit/calculator.go.
- `2b1949c` **feat: /profile flow** — НМТ + квоти + РК + творчий, всі inline.
- `207d95c` **feat: clickable applicants** — список як inline buttons, детальний екран, abit-poisk історія.
- `c108299` **refactor(bot): all-inline UX foundation** — typed callbacks, error middleware, persistent FSM у SQLite.
- `406978d` feat(config): .env loader + .env.example
- `f42915f` **feat(bot): Telegram bot MVP** — /start /help /search.

## 2026-05-21 → 22 · Architecture + services

- `8b89081` feat(service): EnrichService
- `8b1f6fb` feat(abit): stats — Summary, Distribution, Rank, Percentile, RealCompetitors
- `720d883` feat(abit): refactor Quotas/Coefficients → []string + Filter
- `8300bed` feat(service): Program + Applicant pipelines з cache-through
- `57eadcb` **feat: SQLite storage layer** (sqlc + modernc + embedded migrations)
- `2f829b8` **feat(abit): port decoder + links з Python оригіналу**
- `681eec9` **refactor: migrate to Standard Go Layout з Source interface**

## 2026-05-21 і раніше · Початок

- `f196b2c` feat: rewrite AbitPoiskFetchApplicantData to Colly (потім замінили на net/http)
- `eb6957a` pkg: rename pkg in go.mod
- `46f52f0` **init: первинна заготовка** — парсер + типи

## Що видно з журналу

1. **40+ коммітів за 3 дні** (21-23 травня). Це швидкий бій. Архітектура витримала.
2. **Аудит дав 40 findings, з них ~25 фікснули за 5 commits**. Resт — у [[99 — Технічний борг]].
3. **EDBO формула знайдена через Playwright reverse**, не через документацію (її нема). → [[06 — EDBO Research]]
4. **Жоден коміт не "Co-Authored-By: Claude"** — оператор явно попросив прибрати після одного інциденту. Memory зберігає це правило.

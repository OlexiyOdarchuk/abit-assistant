# AbitAssistant — приклад конфігурації.
# Скопіюй у .env (`cp .env.example .env`) і заповни значення.
# Файл .env у git ігнорується.

# ─── Telegram ────────────────────────────────────────────────────────────
# Токен бота від @BotFather. Обов'язково.
TELEGRAM_TOKEN=your_token_here

# Список TelegramID адміністраторів через кому. Не обов'язково.
# Адміни матимуть доступ до /admin команд (broadcast, stats, refresh).
ADMIN_IDS=

# ─── Persistence (PostgreSQL) ────────────────────────────────────────────
# Connection URL до Postgres. На хостингу — встав те, що дає керована БД.
# Локально через docker compose це задано автоматично (сервіс postgres),
# тож тут можна не чіпати. Дефолт для запуску без compose:
#   postgres://abit:abit@localhost:5432/abit?sslmode=disable
# Схема створюється автоматично на старті (міграції), БД може бути порожня.
# DATABASE_URL=postgres://user:pass@host:5432/dbname?sslmode=require

# ─── Logging ─────────────────────────────────────────────────────────────
# Рівень: debug | info | warn | error. Дефолт: info.
LOG_LEVEL=info
